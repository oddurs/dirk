@AGENTS.md

The brief is in AGENTS.md, which every agent reads; there is one copy so the
two cannot drift.

The commands in `.claude/commands/` are the loop — `/work`, `/check`, `/ship`,
`/land`, `/release`, `/backlog` — and the subagents in `.claude/agents/` are
the specialists it delegates to.
